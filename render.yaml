services:
  - type: web
    name: anket-tarama-backend
    runtime: python
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.11
      - key: PYTHONPATH
        value: .
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
      - key: MAX_MANUAL_REVIEW_QUESTIONS
        value: 4
      - key: ADMIN_TOKEN
        sync: false
      # Do not set OMR_DEBUG_ENABLED in production; debug images consume disk.

  - type: web
    name: anket-tarama-frontend
    runtime: node
    region: frankfurt
    plan: free
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npm run start -- --hostname 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_VERSION
        value: 24.5.0
      - key: NEXT_PUBLIC_API_BASE_URL
        sync: false
